import React from 'react';
import { Typography } from '@mui/material';

const CameraDetails = () => (
  <>
    <Typography variant="h5" gutterBottom>Camera Details</Typography>
    <div>Camera Details content goes here.</div>
  </>
);

export default CameraDetails; 